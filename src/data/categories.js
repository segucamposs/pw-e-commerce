// Static list of product categories used for the filter pills in TiendaView.
// 'todos' is a UI-only sentinel that means "show all" — it is not stored in the DB.
export const CATEGORIES = ['todos', 'remeras', 'buzos', 'accesorios'];
